# Пример codemod с использованием jscodeshift

старт миграции:

`nvm use`

`npm install`

`npm run migration` 

после запуска миграции все файлы в папке `src` будут преобразованы согласно скрипту миграции в файле `migration/index.ts`

### Контакты

http://t.me/usyninis_dev



